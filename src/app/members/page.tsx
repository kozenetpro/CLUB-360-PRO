import MembersView from "@/components/pages/MembersView";
import { DEFAULT_LOCALE } from "@/i18n/locales";

export default function MembersPage() {
  return <MembersView locale={DEFAULT_LOCALE} />;
}
